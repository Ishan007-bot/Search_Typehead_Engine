# Keep the build context lean.
__pycache__/
*.py[cod]
.venv/
venv/
env/

# The DB is regenerated inside the image by load_data.py — don't ship the local one.
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

.git/
.gitignore
.vscode/
.idea/
*.md
