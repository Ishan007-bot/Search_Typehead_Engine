# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# SQLite database (generated by load_data.py — not source)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Raw datasets are NOT committed — only the processed data/queries.csv is tracked.
# (The raw ORCAS file is ~1.8 GB; regenerate queries.csv with prepare_orcas.py.)
data/orcas.tsv
data/orcas*.tsv
data/*.tsv.gz
data/*_full.csv
data/kaggle*.csv

# Private study notes — not part of the submission
VIVA_NOTES.md

# IDE / editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
